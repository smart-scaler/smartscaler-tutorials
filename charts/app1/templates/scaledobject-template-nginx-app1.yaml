# Sample template for a ScaledObject
{{- if .value.smartscaler.enabled }}
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: {{ .value.smartscaler.deploymentName }}-scaler
spec:
  scaleTargetRef:
    kind: Deployment
    name: {{ .value.smartscaler.deploymentName }}
  minReplicaCount: {{ .value.smartscaler.minReplicas }}
  maxReplicaCount: {{ .value.smartscaler.maxReplicas }}
  cooldownPeriod: 60
  pollingInterval: 1
  fallback:
    failureThreshold: 1
    replicas: {{ .value.smartscaler.maxReplicas }}
  advanced:
    horizontalPodAutoscalerConfig:
      behavior:
        scaleDown:
          stabilizationWindowSeconds: 300
          policies:
            - type: Percent
              value: 50
              periodSeconds: 120
  triggers:
    - type: prometheus
      metadata:
        serverAddress: {{ .value.smartscaler.trigger.metadata.serverAddress }}
        metricName: {{ .value.smartscaler.trigger.metadata.metricName }}
        ignoreNullValues: "false"
        query: {{ .value.smartscaler.trigger.metadata.query }}
        threshold: "{{ .value.smartscaler.trigger.metadata.threshold }}"
{{- end }}